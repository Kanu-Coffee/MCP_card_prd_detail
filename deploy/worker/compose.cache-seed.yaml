services:
  worker:
    volumes:
      - type: volume
        source: v109-worker-state
        target: /mnt/cardrag-v109-state
        read_only: true
        volume:
          nocopy: true

volumes:
  v109-worker-state:
    external: true
    name: ${CARDRAG_V109_WORKER_STATE_VOLUME:-cardrag-worker-v109-state}
