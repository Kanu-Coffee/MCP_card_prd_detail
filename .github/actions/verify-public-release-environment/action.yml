name: Verify public release environment
description: Fail closed unless Docker Hub publication has exact single-maintainer safeguards

runs:
  using: composite
  steps:
    - name: Verify dockerhub-public secret and tag boundary
      shell: bash
      run: |
        bash "$GITHUB_ACTION_PATH/verify.sh"
