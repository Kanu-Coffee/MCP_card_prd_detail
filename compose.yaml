# Local convenience only. Production deploys the Worker and MCP with their
# independent Compose projects under deploy/worker and deploy/mcp.
include:
  - path: deploy/worker/compose.yaml
  - path: deploy/mcp/compose.yaml
